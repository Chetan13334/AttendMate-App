import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.attendmate.app',
  appName: 'AttendMate',
  webDir: 'dist',   // ✅ your webDir is correct
};

export default config;
